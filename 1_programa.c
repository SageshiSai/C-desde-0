#include <stdio.h>

int main(void){

    //Realmete el void o el (int argc, char *argv[]) se pide en buenas practias ya que se puede pasar argumentos al programa y si no se pasa mejor
    //poner el void
    printf("Hello Wolrd");

    //ya que el programa especifica que se debe retornar un valor entero se debe retornar un valor entero, por el int del inicio del main
    return 0;
}